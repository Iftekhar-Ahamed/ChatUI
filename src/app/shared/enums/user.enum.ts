export enum UserStatus {
    Online = 'online',
    Offline = 'offline',
}
export enum UserType {
    LoginUser = 1,
    OtherUser = 2
}
export enum GlobalUserStatus{
    Connected = 1,
    NotConnected = 2,
    Requested = 3
}